## 修改 inventory 结构计划

### 1. 修改 data.json 结构

* 将根级 "inventory" 键重命名为 "assets"（游戏资产）

* 将内部的 "assets" 数组重命名为 "property"（财产）

* 保持其他结构不变：items, property, knowledge, locations 数组

### 2. 修改 engine.js

* **初始化部分**：

  * 将 inventory 初始化改为 assets 初始化

  * 将 assets 数组初始化改为 property 初始化

* **添加方法**：

  * addItem: 将 `this.gameData.inventory` 改为 `this.gameData.assets`

  * addAsset: 将 `this.gameData.inventory.assets` 改为 `this.gameData.assets.property`

  * addKnowledge: 将 `this.gameData.inventory` 改为 `this.gameData.assets`

  * addLocation: 将 `this.gameData.inventory` 改为 `this.gameData.assets`

* **删除方法**：

  * removeItem: 修改引用路径

  * removeAsset: 将 `this.gameData.inventory.assets` 改为 `this.gameData.assets.property`

  * removeKnowledge: 修改引用路径

  * removeLocation: 修改引用路径

* **更新方法**：

  * updateItem: 修改引用路径

  * updateAsset: 将 `this.gameData.inventory.assets` 改为 `this.gameData.assets.property`

  * updateKnowledge: 修改引用路径

  * updateLocation: 修改引用路径

### 3. 修改相关提示词文件

* **full\_prompt.json**：更新所有相关术语

* **prompt\_storyteller.json**：更新游戏资产相关说明

* **prompt\_recorder.json**：更新资产和财产相关的使用说明

* promptSimplified：更新所有相关术语

### 4. 验证修改

* 确保所有方法调用和引用都已正确更新

* 检查是否有遗漏的引用

* 验证系统功能是否正常运行

### 关键注意事项

* 保持所有方法的功能不变，仅修改引用路径

* 确保术语一致性：assets=游戏资产，property=财产

* 验证修改后系统能够正常初始化和运行

